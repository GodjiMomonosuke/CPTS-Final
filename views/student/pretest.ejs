<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel='stylesheet' href='stylesheets/home1.css' />
    <link href="https://fonts.googleapis.com/css2?family=Prompt:wght@400;600&display=swap" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.5.0/Chart.min.js"></script>
    
    <style>
        body {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #f5f7fa 0%, #e4eaf1 100%);
            margin: 0;
            font-family: 'Prompt', sans-serif;
            padding-bottom: 100px; /* เพิ่มระยะห่างด้านล่าง */
        }

        .container {
            background-color: white;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
            padding: 4rem 3rem; /* เพิ่มความสูงของ container */
            max-width: 600px;
            width: 90%;
            text-align: center;
            transition: transform 0.3s ease;
            margin-top: -100px; /* ขยับขึ้นด้านบน */
        }

        .container:hover {
            transform: translateY(-5px);
        }

        h2 {
            color: #2c3e50;
            font-size: 2.8rem; /* เพิ่มขนาดตัวอักษร */
            margin-bottom: 1rem; /* เพิ่มระยะห่าง */
            font-weight: 600;
            letter-spacing: -0.5px;
        }

        .description {
            color: #6c757d;
            font-size: 1.2rem; /* เพิ่มขนาดตัวอักษร */
            margin-bottom: 3rem; /* เพิ่มระยะห่าง */
            line-height: 1.6;
            padding: 0 20px;
        }

        .btn-secondary {
            background: linear-gradient(45deg, #3498db, #2980b9);
            color: white;
            border: none;
            padding: 15px 40px; /* เพิ่มความกว้างปุ่ม */
            border-radius: 50px;
            font-size: 1.2rem;
            font-weight: 500;
            transition: all 0.3s ease;
            margin-right: 15px;
            box-shadow: 0 4px 15px rgba(52, 152, 219, 0.2);
        }

        .btn-secondary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(52, 152, 219, 0.3);
            background: linear-gradient(45deg, #2980b9, #3498db);
        }

        .btn-skip {
            background-color: #f8f9fa;
            color: #495057;
            border: none;
            padding: 15px 40px; /* เพิ่มความกว้างปุ่ม */
            border-radius: 50px;
            font-size: 1.2rem;
            font-weight: 500;
            transition: all 0.3s ease;
            text-decoration: none;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
        }

        .btn-skip:hover {
            background-color: #e9ecef;
            color: #212529;
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.08);
        }

        .button-container {
            margin-top: 2rem;
        }

        @media (max-width: 768px) {
            .container {
                padding: 3rem 2rem;
                margin-top: -50px;
            }

            h2 {
                font-size: 2.2rem;
            }

            .description {
                font-size: 1.1rem;
                padding: 0;
            }

            .btn-secondary, .btn-skip {
                padding: 12px 30px;
                font-size: 1.1rem;
            }
        }
    </style>
    
    <title>Guide</title>
</head>

<body>
    <div id="content" class="container">
        <h2>GeekCodeCraft</h2>
        <p class="description">แพลตฟอร์มการเรียนรู้ทางออนไลน์ที่สร้างขึ้นเพื่อพัฒนาทักษะการเขียนโปรแกรมและความคิดสร้างสรรค์</p>

        <%# START %> 
        <form action="/pretestSubmit" method="post" autocomplete="off">
            <%# button %> 
                
        <%# content %> 
            <div class="accordion accordion-flush" id="accordionMain">
            <%# %>
                <div id="test0" class="accordion-collapse collapse show" aria-labelledby="flush-headingOne" data-bs-parent="#accordionMain">
                    <div class="button-container">
                        <button type="submit" class="btn btn-secondary">เข้าใช้งาน</button>  
                        <a href="/auth/logout" class="btn btn-skip">กลับ</a>
                    </div>
                </div>
            </div>
        <%# END %>
        </form>
    </div>
</body>
</html>